var documenterSearchIndex = {"docs":
[{"category":"section","location":"manual/visualisation/#Mesh-Visualisation","page":"Visualisation","text":"The plot_mesh_normals function renders the mesh with element outlines and normal arrows coloured by physical group. It is provided as an optional extension — loading Plots triggers it automatically.","title":"Mesh Visualisation"},{"category":"section","location":"manual/visualisation/#Setup","page":"Visualisation","text":"using Plots\nusing RadiativeViewFactor\n\nIf Plots is not loaded, calling plot_mesh_normals raises a clear error message rather than a cryptic MethodError.","title":"Setup"},{"category":"section","location":"manual/visualisation/#Basic-usage","page":"Visualisation","text":"mesh = load_mesh(\"geometry.msh\"; surface_dim=1)\nfig  = plot_mesh_normals(mesh)\n\nThe normal scale is estimated automatically from the mesh bounding box. For 2D curve meshes the figure uses a 2D Axis; for 3D surface meshes it uses Axis3.","title":"Basic usage"},{"category":"section","location":"manual/visualisation/#Options","page":"Visualisation","text":"fig = plot_mesh_normals(mesh;\n    normal_scale  = 0.05,              # arrow length in mesh units\n    show_nodes    = true,              # scatter-plot element nodes\n    show_indices  = true,              # label each element with its index\n    group_colors  = Dict(1=>:red,      # override colours per physical group tag\n                         2=>:blue),\n    backend_3d    = true)              # force Axis3 even for curve meshes","title":"Options"},{"category":"section","location":"manual/visualisation/#Saving-to-file","page":"Visualisation","text":"fig = plot_mesh_normals(mesh)\nsavefig(fig, \"normals.pdf\")\nsavefig(fig, \"normals.png\")","title":"Saving to file"},{"category":"section","location":"manual/visualisation/#Interpreting-the-output","page":"Visualisation","text":"Each element is drawn as:\n\nA line (Line2, Line3) or edge outline (Tri3, Quad4, Tri6, Quad8) in the group colour\nAn arrow at the element centre pointing in the outward normal direction\n\nArrows pointing toward the opposing surface indicate correct orientation. Arrows pointing away indicate a winding error — use reverse_normals=true in load_mesh to flip all normals before computing view factors.","title":"Interpreting the output"},{"category":"section","location":"manual/integration_methods/#Integration-Methods","page":"Integration Methods","text":"Four integration strategies are available, selected per call to compute_view_factors. They differ in accuracy, cost, and which geometric configurations they handle well.","title":"Integration Methods"},{"category":"section","location":"manual/integration_methods/#Gauss–Legendre-quadrature-(default)","page":"Integration Methods","text":"result = compute_view_factors(mesh; nquad=4)\n\nThe double surface integral is evaluated at a tensor product of nquad × nquad Gauss–Legendre points on each element pair. This is the default method and is appropriate for most geometries.\n\nConvergence: spectral — errors decrease as O(exp(-c·nquad)) for smooth integrands. A good starting point is nquad=4 — that is nquad² = 16 points on each element, so nquad⁴ = 256 point-pairs per element pair; increase to nquad=6 or nquad=8 for finer accuracy or larger elements.\n\nLimitations: convergence degrades near corner singularities where two elements share a vertex or edge. In these cases use the Duffy transformation.","title":"Gauss–Legendre quadrature (default)"},{"category":"section","location":"manual/integration_methods/#Duffy-transformation","page":"Integration Methods","text":"result = compute_view_factors(mesh; nquad=6, use_duffy=true)\n\nFor same-order quad element pairs (Quad4–Quad4 or Quad8–Quad8) sharing a vertex or edge, the 1/r² singularity in the view factor kernel is integrable but poorly resolved by standard quadrature. The Duffy transformation introduces a radial coordinate whose Jacobian cancels the singularity, leaving a smooth integrand that Gauss quadrature resolves efficiently.\n\nSingularity type is detected automatically:\n\nNo shared nodes → standard quadrature (unchanged)\nOne shared corner node → 4-region \"biggest-coordinate\" Duffy decomposition\nTwo shared corner nodes (one edge) → 6-region decomposition (the shared edge split into two triangles, each further split 3 ways)\n\nThis is an elementary generalization of Duffy's original single-simplex transform, not the specific region formulas from Sauter & Schwab's boundary element method (a related but different decomposition of the same singularity) — see Theory for the exact construction.\n\nConstraints:\n\nCPU only (use_duffy is ignored on GPU backends with a warning)\nQuad4–Quad4 and Quad8–Quad8 pairs only; Tri6 and Line3 pairs always use standard quadrature\nIncompatible with monte_carlo=true (but see the note below — the pair-area Monte Carlo path always applies this same Duffy patch to near/touching pairs regardless of use_duffy)\nNot applicable for surface_dim=1 (the 2D 1/r singularity at shared endpoints is physically divergent and cannot be regularized)\n\nCost: 4 × nquad⁴ evaluations per vertex pair, 6 × nquad⁴ per edge pair, versus nquad⁴ for standard quadrature. Since only adjacent pairs trigger the Duffy path, the overhead depends on mesh topology. A lower nquad (e.g. 4) with use_duffy=true typically outperforms a higher nquad (e.g. 16) with standard quadrature for inclined-plate geometries.\n\n.re2 meshes are always Quad4, so use_duffy=true is recommended whenever a structured hex-mesh boundary has many edge-adjacent Quad4 pairs — plain quadrature overestimates their view factor.","title":"Duffy transformation"},{"category":"section","location":"manual/integration_methods/#Monte-Carlo-(pair-area-sampling)","page":"Integration Methods","text":"result = compute_view_factors(mesh; monte_carlo=true, n_samples=50000)\n\nEach element pair is estimated from n_samples stratified sample pairs. Samples are placed on a ⌊√N⌋ × ⌊√N⌋ grid of strata within the reference element, giving O(1/N) variance convergence rather than O(1/√N) for plain Monte Carlo.\n\nBecause the 1/r² kernel's variance diverges for touching or near-touching pairs, compute_view_factors always patches every near/touching pair (found via a spatial grid, see factor) with the deterministic Duffy value after the Monte Carlo bulk finishes — use_duffy has no separate effect when monte_carlo=true, since this patch is unconditional.\n\nFor efficiency, one independent stratified sample set is drawn once per element and reused across that element's pairings (the diagonal self-pair, with self_vf=true, draws a fresh second set). Within any pair the two elements' samples are still independent, so each entry's estimate remains unbiased with the stated variance; only estimates in the same row/column become correlated. This reuse is what makes the MC path ~12–15× faster than re-sampling both elements per pair — see the benchmarks/ directory.\n\nWhen to use:\n\nNear-singular pairs where MC variance is still finite (unlike the 1/r² case where variance diverges — use Duffy instead)\nRapid approximate estimates at low n_samples\nVery large meshes on GPU, where the O(N²) bulk parallelizes well\n\nNot for obstructed geometries, despite the intuition that MC should win there. Both paths apply the identical K == 0 guard before calling is_visible, so both ray-cast exactly once per kernel-positive point-pair — MC has no structural advantage. It simply evaluates more point-pairs: n_samples versus nquad⁴ (256 at the default nquad=4), so n_samples=5000 issues ~20x the ray casts. Measured on a 4320-element obstructed reactor-pin case, CPU: 1368 s at n_samples=5000 versus 60 s at nquad=4, with the two answers agreeing to five decimals (F = 0.380439 vs 0.380440). The GPU backend does not rescue this — the per-sample BVH traversal is branch-divergent, and the same case extrapolated to ~51 min on an M3 versus 22.8 min on 8 CPU threads. For large obstructed meshes, ray-shooting Monte Carlo (below) is faster on both CPU and GPU because it does not re-check obstruction per pair.\n\nReproducibility: pass an explicit RNG for deterministic results:\n\nusing Random\nresult = compute_view_factors(mesh; monte_carlo=true, n_samples=50000,\n                               rng=MersenneTwister(42))\n\nGPU: each thread uses an independent xorshift64 pseudo-random stream seeded from the global seed plus the thread index. The rng keyword is ignored on GPU; a random global seed is generated on the host at each call.\n\nConstraints:\n\nn_samples applies per element pair, not to the whole geometry\nIncompatible with use_duffy=true","title":"Monte Carlo (pair-area sampling)"},{"category":"section","location":"manual/integration_methods/#Monte-Carlo-(ray-shooting)","page":"Integration Methods","text":"result = compute_view_factors(mesh; raytrace=true, n_rays=10000)\n\nA different Monte Carlo method from pair-area sampling above, not a faster version of it. Instead of sampling point pairs on each element pair (O(N²) pairs × n_samples), this shoots n_rays cosine-weighted rays per element and tallies which element each first hits, using one BVH built over the whole radiating mesh (O(N · nrays · log N)). Because every radiating element is already in that BVH, radiating elements obstruct each other automatically — `obstructiongroupsis not needed for self-shadowing within the radiating set and only adds *extra* non-radiating blocker geometry here. There is also no adjacent-pair singularity (it never evaluates1/r²), souse_duffy` and the near-pair patch do not apply.\n\nReciprocity is enforced by construction: each unordered pair's two independent ray-based estimates (one from each side) are averaged, so results are exactly symmetric despite being stochastic. This is also why row sums on a closed enclosure are only approximately 1 (ordinary MC noise, shrinking with n_rays) rather than exact — every off-diagonal entry blends in the other element's independent estimate too.\n\nRuns on CPU or GPU (its own kernel, one thread per element on GPU, not per pair):\n\nusing CUDA   # or Metal\nresult = compute_view_factors(mesh; raytrace=true, n_rays=10000,\n                               backend=CUDABackend())\n\nConstraints:\n\n3D surface meshes only (mesh.mesh_dim == 2); not available for curve meshes\nIncompatible with self_vf=true (a ray is never tested against its own origin element) and with monte_carlo=true — pick one estimator\nn_rays counts rays per element, not per element pair, so it is not directly comparable to n_samples; tune per case\n\nKnown limitation: on coarsely faceted curved bodies (spheres, cylinders), adjacent element facets can disagree slightly at their shared edge, letting a near-grazing ray clip a neighbouring facet it geometrically shouldn't reach. This produces a small, systematic bias (not ordinary MC noise — it does not shrink with n_rays) that decreases with mesh refinement. See benchmarks/howell/RESULTS.md for quantified examples.\n\nSpeed, on a 72-point subset of the Howell catalog benchmark:\n\nKernel Total seconds vs. ray-shooting\nRay-shooting 193.4 1×\nQuadrature 555.9 2.9× slower\nPair-area Monte Carlo (n_samples=5000) 5113.3 26.4× slower","title":"Monte Carlo (ray-shooting)"},{"category":"section","location":"manual/integration_methods/#Choosing-a-method","page":"Integration Methods","text":"Situation Recommended method\nSmooth geometry, well-separated surfaces Quadrature (nquad=4–8)\nInclined plates with shared edge Duffy (nquad=4–6)\nLarge or obstructed 3D mesh Ray-shooting Monte Carlo (n_rays=10000)\nQuick estimate, curve mesh, or self-view factors Pair-area Monte Carlo (n_samples=5000)\nGPU computation Quadrature or either Monte Carlo variant","title":"Choosing a method"},{"category":"section","location":"manual/gpu/#GPU-Backends","page":"GPU Backends","text":"RadiativeViewFactor.jl supports NVIDIA GPUs via CUDA.jl and Apple Silicon GPUs via Metal.jl. Both backends are optional weak dependencies loaded automatically when the user loads the corresponding package.","title":"GPU Backends"},{"category":"section","location":"manual/gpu/#NVIDIA-(CUDA)","page":"GPU Backends","text":"using CUDA\nusing RadiativeViewFactor\n\nresult = compute_view_factors(mesh; nquad=4, backend=CUDABackend())\n\nCUDA runs in Float64 throughout. All features except use_duffy and surface_dim=1 are supported.","title":"NVIDIA (CUDA)"},{"category":"section","location":"manual/gpu/#Apple-Silicon-(Metal)","page":"GPU Backends","text":"using Metal\nusing RadiativeViewFactor\n\nresult = compute_view_factors(mesh; nquad=4, backend=MetalBackend())\n\nMetal runs in Float32 internally; results are promoted to Float64 before aggregation and returned as Float64. Apple GPUs do not support Float64 natively. For geometries with near-grazing element pairs or coordinates spanning many orders of magnitude, Float32 rounding (~1e-7 relative) may introduce small errors in individual element-pair values that are generally negligible after group-level aggregation.","title":"Apple Silicon (Metal)"},{"category":"section","location":"manual/gpu/#Monte-Carlo-(pair-area)-on-GPU","page":"GPU Backends","text":"Both backends support pair-area Monte Carlo integration. Each GPU thread uses an independent xorshift64 pseudo-random stream derived from a per-call global seed and the thread index. The rng keyword is ignored on GPU backends.\n\nresult = compute_view_factors(mesh; monte_carlo=true, n_samples=50000,\n                               backend=CUDABackend())","title":"Monte Carlo (pair-area) on GPU"},{"category":"section","location":"manual/gpu/#Monte-Carlo-(ray-shooting)-on-GPU","page":"GPU Backends","text":"Ray-shooting Monte Carlo (raytrace=true) has its own GPU kernel, one thread per element rather than per element pair — each thread shoots n_rays cosine-weighted rays from its element into a scene BVH covering the whole radiating mesh, so obstruction among radiating elements is automatic:\n\nresult = compute_view_factors(mesh; raytrace=true, n_rays=10000,\n                               backend=CUDABackend())\n\nIt is incompatible with self_vf=true and with monte_carlo=true, and is 3D-surface-mesh only. See Integration Methods for the method itself and its known faceting-bias limitation on coarsely meshed curved bodies.","title":"Monte Carlo (ray-shooting) on GPU"},{"category":"section","location":"manual/gpu/#Obstruction-on-GPU","page":"GPU Backends","text":"Obstruction detection is fully supported on GPU. The BVH is built on the CPU, flattened to typed arrays, and uploaded to the device once before the kernel launch. The GPU kernel uses stackless BVH traversal (no per-thread stack memory) for reduced register pressure.\n\nresult = compute_view_factors(mesh; nquad=4, backend=CUDABackend(),\n                               obstruction_groups=[3, 4])","title":"Obstruction on GPU"},{"category":"section","location":"manual/gpu/#Constraints","page":"GPU Backends","text":"surface_dim=1 (curve meshes) is not supported on GPU; use CPU()\nuse_duffy=true is CPU-only; it is silently ignored on GPU backends\nself_vf=true is CPU-only, and is also incompatible with raytrace=true on any backend","title":"Constraints"},{"category":"section","location":"manual/gpu/#Performance-crossover","page":"GPU Backends","text":"GPU backends outperform CPU (8 threads) approximately when:\n\nMethod Elements N\nQuadrature N ≳ 300–500\nMonte Carlo (pair-area) N ≳ 200\n\nRay-shooting Monte Carlo has not had a CPU/GPU crossover point measured this way; on CPU it already outperforms both other methods by 3–25× on the Howell benchmark suite (see Integration Methods), so a GPU comparison would need its own benchmark rather than reusing the pair-area numbers above.\n\nFor smaller meshes, kernel JIT compilation and host↔device data transfer dominate the runtime. Metal is typically 2–5× slower than a comparable NVIDIA GPU for this workload due to Float32 vs Float64 and lower compute throughput.","title":"Performance crossover"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"","title":"API Reference"},{"category":"section","location":"api/#Mesh-I/O","page":"API Reference","text":"","title":"Mesh I/O"},{"category":"section","location":"api/#View-factor-computation","page":"API Reference","text":"","title":"View factor computation"},{"category":"section","location":"api/#Post-processing","page":"API Reference","text":"","title":"Post-processing"},{"category":"section","location":"api/#Nek5000/NekRS-export","page":"API Reference","text":"","title":"Nek5000/NekRS export"},{"category":"section","location":"api/#Visualisation","page":"API Reference","text":"","title":"Visualisation"},{"category":"function","location":"api/#RadiativeViewFactor.MeshIO.load_mesh","page":"API Reference","text":"load_mesh(filename; surface_dim=2, verbose=true) -> MeshData\n\nLoad a mesh and extract all supported 1st- or 2nd-order elements belonging to named physical groups.\n\nAny file format that the Gmsh API can open is supported — the format is inferred from the extension. This includes Gmsh .msh (all versions), and via Gmsh's importers .stl, .step/.stp, .bdf/.nas (Nastran), .med, .vtk (legacy ASCII unstructured grids), and others.\n\nSupported element families (any mix in one mesh):\n\nSurfaces (surface_dim=2): Tri3, Quad4 (1st order); Tri6, Quad8/Quad9 (2nd order).\nCurves   (surface_dim=1): Line2 (1st order); Line3 (2nd order).\n\nsurface_dim=2 (default) — surface mesh, 3D view factors. surface_dim=1            — planar curve mesh, 2D view factors.\n\nNamed physical groups are used to partition the radiating surfaces. Formats that cannot carry physical groups (e.g. STL) have no named groups; in that case a single synthetic group named \"default\" covering every entity of surface_dim is created so the rest of the pipeline behaves uniformly.\n\nXML VTK files (.vtu and XML-form .vtk) are detected automatically and read through ReadVTK.jl (a weak dependency — using ReadVTK must be in scope). Legacy ASCII/binary .vtk files are not handled by ReadVTK and fall through to the Gmsh importer. See load_vtu for VTK-specific options.\n\nNek5000/NekRS .re2 binary meshes are detected by extension and read by a dedicated in-tree parser (Gmsh cannot open them). The 3D hex volume mesh's boundary faces become radiating Quad4 surfaces grouped by Nek boundary-condition label. See load_re2.\n\n\n\n\n\n","title":"RadiativeViewFactor.MeshIO.load_mesh"},{"category":"function","location":"api/#RadiativeViewFactor.MeshIO.load_vtu","page":"API Reference","text":"load_vtu(filename; surface_dim=2, reverse_normals=false, verbose=true,\n         group_field=nothing) -> MeshData\n\nLoad an XML VTK unstructured grid (.vtu) through ReadVTK.jl. Requires using ReadVTK to be in scope (ReadVTK is a weak dependency).\n\nVTK has no native concept of physical groups. If group_field names a per-cell integer data array it is used to partition elements into groups; otherwise (or if the named array is absent) a single \"default\" group is created. Common region arrays (CellEntityIds, gmsh:physical, RegionId, MaterialIds) are tried automatically when group_field === nothing.\n\nCell types are mapped to element families as: Line(3)→line2, QuadraticEdge(21)→line3, Triangle(5)→tri3, QuadraticTriangle(22)→tri6, Quad(9)→quad4, QuadraticQuad(23)→quad8.\n\n\n\n\n\n","title":"RadiativeViewFactor.MeshIO.load_vtu"},{"category":"function","location":"api/#RadiativeViewFactor.MeshIO.load_re2","page":"API Reference","text":"load_re2(filename; surface_dim=2, reverse_normals=false, verbose=true) -> MeshData\n\nLoad a Nek5000/NekRS .re2 binary mesh. The 3D hex volume mesh's boundary faces become radiating Quad4 surfaces, grouped by their Nek boundary-condition label. Only genuinely internal faces (cbc = 'E' or blank) are skipped; periodic ('P') faces are kept as their own group, matching Nek5000/NekRS's own view-factor convention of including every non-internal boundary face in the radiation enclosure so that view factors close (row sums to 1) — solid walls vs. other boundaries is a runtime (cbc.eq.'W  ') distinction made by the case's .usr file, not something this loader can determine from .re2 labels alone. Each element's Nek global element number and local face index (1-6) are recorded in SurfaceElement.eg/.iface for round-tripping view factors back into Nek5000's (element, face) bookkeeping — see write_nekrs_view_factors. Word size (4- or 8-byte reals) and byte order are auto-detected. Only surface_dim=2 (3D → surfaces) is supported.\n\n\n\n\n\n","title":"RadiativeViewFactor.MeshIO.load_re2"},{"category":"type","location":"api/#RadiativeViewFactor.MeshIO.MeshData","page":"API Reference","text":"MeshData\n\nFields\n\ncoords         : (3 × N_nodes) coordinate matrix\nsurface_elems  : vector of SurfaceElement (all radiating curves/surfaces)\ngroup_tags     : Dict tag → name\ngroup_elems    : Dict tag → element indices (into surface_elems)\ngroup_tri_soup : Dict tag → obstruction geometry for that group.                    For surface meshes (meshdim=2): (3, 3, Ntris) triangle soup.                    For curve meshes  (meshdim=1): (3, 2, Nsegs) segment soup,                      axis 1 = xyz, axis 2 = endpoint (1 or 2), axis 3 = segment.\nmesh_dim       : 1 for curve meshes, 2 for surface meshes\n\n\n\n\n\n","title":"RadiativeViewFactor.MeshIO.MeshData"},{"category":"type","location":"api/#RadiativeViewFactor.MeshIO.SurfaceElement","page":"API Reference","text":"SurfaceElement(nodes, group, family, eg=0, iface=0, phys_tag=0)\n\neg and iface are the Nek5000/NekRS global element number and local face index (1-6) this element corresponds to, populated by load_re2 so that per-element view factors can be written back out in Nek's own (element, face) bookkeeping (see write_nekrs_view_factors). They default to 0 for elements loaded from any other mesh format, where the concept doesn't apply.\n\nphys_tag is the original numeric Gmsh Physical Surface tag for this face (Nek's bc(5,ifc,iel,1), the same value usrdat2 reads), also populated by load_re2 (0 otherwise). It exists because group/ group_tags are keyed by the Nek boundary-condition code ('W', 'P', or a generic placeholder like \"MSH\" that gmsh2nek writes for every ordinary, non-periodic surface) — meshes with several differently-named Physical Surfaces that all end up with the same generic code (e.g. two concentric spheres, or a pebble bed's pebble/duct-wall surfaces) are otherwise indistinguishable by group alone, even though usrdat2 can tell them apart. Use split_groups_by_tag to regroup a mesh at (code, phys_tag) granularity when you need to select one such surface as an obstruction_groups entry.\n\n\n\n\n\n","title":"RadiativeViewFactor.MeshIO.SurfaceElement"},{"category":"function","location":"api/#RadiativeViewFactor.Assembly.compute_view_factors","page":"API Reference","text":"compute_view_factors(mesh; nquad=4, obstruction_groups=Int[],\n                     radiating_groups=Int[], backend=CPU(),\n                     self_vf=false, facing_cull=true,\n                     raytrace=false, n_rays=10000, verbose=true)\n                     -> ViewFactorResult\n\nAssemble the full view factor matrix at element and physical-group level.\n\nArguments\n\nmesh                : MeshData returned by load_mesh\nnquad               : Gauss points per direction; nquad² points on each                         surface element (nquad on each curve element), so                         the pair integral costs nquad⁴ point-pairs per                         surface element pair (nquad² per curve pair) —                         256 point-pairs at the default nquad=4. Compare                         n_samples below, which counts point-pairs directly.                         When                         monte_carlo=true, only used for the adjacent-pair                         Duffy patch (see monte_carlo below), not the bulk                         sampling.\nobstruction_groups  : physical group tags that may occlude rays. Source and                         destination groups are excluded automatically per pair.                         Not applied to the Monte Carlo adjacent-pair patch —                         two elements sharing a vertex/edge cannot have a                         third surface positioned between them.\nradiating_groups    : restrict the radiating surface to these physical                         group tags; every other group still obstructs (if                         listed in obstruction_groups) but is not itself                         assembled. Empty (default) means every group radiates,                         the previous behaviour. Because assembly is dense over                         whatever it is handed, this turns an O(N_total²)                         problem into O(N_radiating²): asking for one pair of                         surfaces in a mesh full of shadowing bodies no longer                         computes the view factors between those shadowing                         bodies. On a reactor fuel-assembly slice, FA_wall →                         Pin_11 with 19 shadowing pins loaded drops from 18400                         radiating elements to 3040 — 37x fewer pairs.                         The returned F_group covers only the radiating                         groups. The enclosure is deliberately open, so row                         sums no longer approach 1 and check_closure is not                         meaningful; reciprocity is unaffected. See                         restrict_to_radiating.\nbackend             : CPU() (default), CUDABackend(), or MetalBackend()\nself_vf             : include self view factors (concave elements). CPU only.\nmonte_carlo         : use stratified Monte Carlo area-sampling instead of                         Gauss–Legendre quadrature for the O(N²) bulk of                         element pairs — the fastest option for large meshes,                         especially on GPU. The 1/r² kernel has unbounded                         variance for pairs sharing a vertex or edge, so no                         amount of sampling fixes those; this function                         therefore always patches those O(N) pairs                         afterward with the deterministic Duffy transform                         (CPU-only, using nquad above), on top of                         whichever backend the bulk ran on. use_duffy has                         no separate effect here — the patch is                         unconditional whenever monte_carlo=true.\nn_samples           : MC sample pairs per element pair (default 5000).                         Ignored when monte_carlo=false. Variance decreases                         as O(1/N) for the (non-adjacent) pairs Monte Carlo                         handles. This counts point-pairs directly, so it is                         nquad⁴ — not nquad² — that it should be compared                         against: n_samples=5000 does ~20x the kernel                         evaluations of the default nquad=4, and with                         obstruction_groups set, ~20x the BVH ray casts,                         since both paths ray-cast once per kernel-positive                         point-pair. On CPU that made a 4320-element                         obstructed case take 1368 s at n_samples=5000                         versus 60 s at nquad=4, for answers agreeing to 5                         decimals. The default of 5000 is the value used                         throughout benchmarks/howell/ (115 catalog points,                         96% within 1% of the published value, median error                         0.008%) — at that sample count the MC estimator's                         own noise is typically 10-1000x smaller than the                         mesh's discretization error against the analytic                         answer, so raising it further mostly buys accuracy                         the mesh cannot use. Lower it for a quick look at a                         large mesh; raise it only if n_samples sweeps on                         your own geometry show the noise still dominates.\nrng                 : RNG for the CPU MC path. Pass a seeded RNG (e.g.                         MersenneTwister(42)) for reproducible results.                         Ignored on GPU.\nuse_duffy           : apply the Duffy singularity transformation (see                         DuffyKernel.jl) for same-order quad pairs (Quad4                         or Quad8) sharing a vertex or edge, in the plain                         quadrature path. CPU only. Ignored when                         monte_carlo=true (that path always Duffy-patches                         adjacent pairs regardless of this flag).\nfactor              : near-pair patch radius, in element diameters (see                         near_pairs in DuffyKernel.jl) — only used when                         monte_carlo=true, to decide which O(N) pairs get                         the deterministic Duffy patch instead of the raw                         MC estimate. Raising it does not, by itself, make                         those pairs more accurate — nquad (above) is                         what governs the patch's own resolution, and is                         the more effective lever for closure error on                         meshes with large or elongated elements.\nraytrace            : use ray-shooting Monte Carlo (RayTraceKernel.jl)                         instead of either quadrature or the pair-area                         monte_carlo above. For each element, shoots                         n_rays cosine-weighted rays and tallies which                         element each first hits, via one BVH over the                         whole radiating mesh — O(N·nrays·log N) instead                         of the O(N²) pair loop the other two paths always                         pay. Obstruction is then a side effect of the same                         query, not a separate check: radiating elements                         obstruct each other automatically (a real                         behavioural difference from the other two paths,                         which only apply obstruction for groups explicitly                         listed in `obstructiongroups— see note 3 below);obstructiongroupshere only adds *extra*,                         non-radiating blocker geometry to the scene. No                         adjacent-pair singularity exists for this                         estimator (it never evaluates 1/r²), souseduffy/the Duffy patch/factordo not apply.facingculldoes not apply either (there is no                         O(N²) pair loop to cull). Runs on CPU or GPU                         (backend=CUDABackend()/MetalBackend(), same asmontecarlo) with its own GPU kernel                         (GPURayTraceKernels.jl); one thread per *element*                         on GPU, not per pair. 3-D meshes only                         (mesh.meshdim == 2); incompatible withselfvf(a ray is never tested against its own                         origin element) and withmontecarlo=true.                         Reciprocity is enforced by construction (each                         unordered pair's two independent ray-based                         estimates — from each side — are averaged), so                         results are exactly symmetric despite being                         stochastic, unlike a raw hit-count would be. This                         same averaging is why row sums on a closed                         enclosure are only approximately 1 (ordinary MC                         noise, shrinking withnrays) rather than exact:                         each element's own rays alone would close exactly,                         but every off-diagonal entry blends in the                         *other* element's independent estimate too. See                         the module docstring inRayTraceKernel.jlfor the                         derivation andchangelog.md` for validation                         against analytic cases and against quadrature                         (including a case where quadrature itself was the                         less accurate one, under-resolving a sharp                         obstruction shadow boundary).\nn_rays              : rays per element (default 10000) when                         raytrace=true; ignored otherwise. Unlike                         n_samples (which counts point-pairs per element                         pair), one ray-shooting pass from an element                         estimates its view factor to every other element                         at once, so this is not directly comparable to                         n_samples — tune per-case rather than assuming                         the same order of magnitude applies.\nfacing_cull         : reject element pairs that provably cannot see each                         other before integrating them (default true). Each                         element gets a conservative bounding box for its points                         and another for its normals (O(N)); a pair whose kernel                         is then provably zero at every point pair is skipped                         in O(1) instead of                         being discovered zero one quadrature point — or one                         MC sample, or one BVH ray cast — at a time. Results                         are unchanged; on closed convex bodies (tube bundles,                         pebble beds) most pairs face away and this is a large                         saving. Set false to disable, e.g. for a mesh whose                         2nd-order elements are curved sharply enough that the                         sampled bounds might not contain them (see                         ElementBounds).\nverbose             : print progress and row-sum diagnostics\n\nReturns\n\nA ViewFactorResult.\n\nExamples\n\nresult = compute_view_factors(mesh; nquad=6)\nresult = compute_view_factors(mesh; nquad=6, use_duffy=true)\nresult = compute_view_factors(mesh; monte_carlo=true, n_samples=50000)               # CPU\nresult = compute_view_factors(mesh; monte_carlo=true, n_samples=50000,\n                               backend=CUDABackend())                                 # GPU — fastest for large meshes\nresult = compute_view_factors(mesh; raytrace=true, n_rays=10000)                     # CPU, fastest for large/obstructed 3D meshes\n\n# Just one pair of surfaces, with everything else still shadowing them:\nall_tags = collect(keys(mesh.group_tags))\nresult = compute_view_factors(mesh; nquad=4,\n                               radiating_groups   = [wall_tag, pin_tag],\n                               obstruction_groups = all_tags)\n\n\n\n\n\n","title":"RadiativeViewFactor.Assembly.compute_view_factors"},{"category":"type","location":"api/#RadiativeViewFactor.Results.ViewFactorResult","page":"API Reference","text":"ViewFactorResult\n\nStores element-level and group-level view factor matrices produced by compute_view_factors.\n\nFields\n\nF_elem      : (Nelem × Nelem) element-level view factor matrix\nA_elem      : N_elem-vector of element areas (or lengths for curve meshes)\nF_group     : (Ngroup × Ngroup) group-level view factor matrix\nA_group     : N_group-vector of group areas/lengths\ngroup_tags  : sorted physical group tags (row/column labels for F_group)\ngroup_names : physical group names in the same order\n\n\n\n\n\n","title":"RadiativeViewFactor.Results.ViewFactorResult"},{"category":"function","location":"api/#RadiativeViewFactor.Results.aggregate_by_group","page":"API Reference","text":"aggregate_by_group(result, mesh) -> (F_group, A_group, tags, names)\n\nRe-aggregate element-level results to group level (useful after modifying result.F_elem).\n\n\n\n\n\n","title":"RadiativeViewFactor.Results.aggregate_by_group"},{"category":"function","location":"api/#RadiativeViewFactor.Results.check_reciprocity","page":"API Reference","text":"check_reciprocity(result; tol=1e-4) -> Bool\n\nVerify Aᵢ Fᵢⱼ ≈ Aⱼ Fⱼᵢ for all element pairs. Prints the maximum relative error and returns true if it is below tol.\n\n\n\n\n\n","title":"RadiativeViewFactor.Results.check_reciprocity"},{"category":"function","location":"api/#RadiativeViewFactor.Results.check_closure","page":"API Reference","text":"check_closure(result; tol=1e-3) -> Bool\n\nVerify that no row of F_elem sums to more than 1 + tol. Prints the row-sum range and returns true if the check passes.\n\nOnly meaningful for a closed enclosure. A result computed with compute_view_factors(...; radiating_groups=...) is deliberately open — the non-radiating bodies absorb their share but are never assembled — so its rows sum to less than 1 by construction and a low row sum is not a defect. Use check_reciprocity, which is unaffected, to validate such a result.\n\n\n\n\n\n","title":"RadiativeViewFactor.Results.check_closure"},{"category":"function","location":"api/#RadiativeViewFactor.NekExport.write_nekrs_view_factors","page":"API Reference","text":"write_nekrs_view_factors(path, result, mesh;\n                          tol=0.0, max_visible_walls=10_000,\n                          max_walls=4_000_000, verbose=true) -> Int\n\nWrite result.F_elem to path in the list-directed ASCII format read by Nek5000/NekRS's vf_read_view_factors (in the Nek-VF view_factors.f module):\n\nnwalls\niw₁ ieg₁ ifc₁ nvwalls₁\n  jw ieg ifc Fij        (nvwalls₁ lines)\niw₂ ieg₂ ifc₂ nvwalls₂\n  ⋮\n\niw runs 1:N in mesh.surface_elems order (Nek5000 does not require any particular enclosure-face numbering — it rebuilds its own (eg,ifc) → iw map from this file's ieg/ifc columns at load time). ieg/ifc are read from SurfaceElement.eg/.iface, so mesh must come from load_re2 — an error is raised otherwise.\n\nOnly pairs with F_elem[i,j] > tol are written as visible faces per wall, matching how Nek5000's hemi-cube view-factor calculator only records genuinely visible (rasterized) pairs; tol=0.0 (default) keeps every strictly-positive entry, which for the quadrature/Monte Carlo/Duffy kernels here already excludes back-facing and obstructed pairs (vf_kernel returns exactly 0.0 for those). Raise tol to shrink the file for dense meshes at the cost of a small closure error.\n\nThe same file is used for both open_face_option=1 and =2 in vf_calculate_radiation_heat_flux — the option only changes which faces emit at runtime (cbc.eq.'W  ', decided by the case's .usr file), not which faces appear in this file. This function therefore writes every element in mesh.surface_elems, walls and non-walls (e.g. periodic) alike — see the load_re2 docstring for why non-wall boundary faces must stay in the enclosure.\n\nPrints (does not throw) a warning if any row's visible-face count would exceed max_visible_walls, or if N exceeds max_walls — these mirror the parameters of the same name in view_factors/VIEW_FACTORS and only warn, not abort, on the Fortran side too; increase those parameters if you see this warning and re-run the Nek5000/NekRS case.\n\nReturns N, the number of walls written.\n\n\n\n\n\n","title":"RadiativeViewFactor.NekExport.write_nekrs_view_factors"},{"category":"function","location":"api/#RadiativeViewFactor.plot_mesh_normals","page":"API Reference","text":"plot_mesh_normals(mesh; normal_scale=nothing, group_colors=nothing,\n                  show_nodes=false, show_indices=false, backend_3d=auto)\n    -> Plots.Plot\n\nVisualise mesh elements with normal arrows coloured by physical group.\n\nRequires Plots.jl to be loaded first:\n\nusing Plots\n\nArguments\n\nmesh          : MeshData from load_mesh\nnormal_scale  : arrow length in mesh units. Auto-estimated from the                   bounding box diagonal if omitted.\ngroup_colors  : Dict{Int,Any} mapping physical group tag → any colour                   accepted by Plots.jl (e.g. :red, \"#FF0000\").                   overriding the automatic palette for specified groups.\nshow_nodes    : scatter-plot all element nodes.\nshow_indices  : annotate each element with its index number.\n\nReturns\n\nThe Plots.Plot object; save it with savefig(fig, \"file.png\").\n\nExample\n\nusing Plots, RadiativeViewFactor\nmesh = load_mesh(\"geometry.msh\"; surface_dim=1)\nfig  = plot_mesh_normals(mesh; normal_scale=0.05, show_nodes=true)\n\n\n\n\n\n","title":"RadiativeViewFactor.plot_mesh_normals"},{"category":"section","location":"manual/performance/#Performance-Guide","page":"Performance Guide","text":"","title":"Performance Guide"},{"category":"section","location":"manual/performance/#Assembly-cost-and-quadrature-reuse","page":"Performance Guide","text":"The view factor matrix is dense and assembly is O(N²) in the element count N. To keep the per-pair work minimal, each element's quadrature points, normals, and area elements (deterministic path) — or its stratified Monte Carlo samples (MC path) — are pre-evaluated once per element and reused across every element pair, instead of being rebuilt inside the pair loop.\n\nConcretely, compute_view_factors builds one ElementQuad (or ElementSamples for monte_carlo=true) per element up front, then feeds the cached data into the pair integrator. This removes the O(N²) reconstruction of shape functions and quadrature rules that a naive pairwise evaluation would incur, and for nquad > 5 it also avoids re-running the Golub–Welsch eigensolve per pair (the rule is memoized).\n\nThe change is purely an evaluation-order optimization — results are numerically identical, reciprocity holds to machine precision, and analytic view factors are unchanged. Measured on an 8-core CPU it gives roughly:\n\nPath Speedup Allocation reduction\nDeterministic quadrature ~2.6–3.1× ~60–80×\nMonte Carlo ~12–15× ~350–450×\n\nThe Monte Carlo path benefits more because it previously re-sampled both elements on every pair. Reproducible benchmark scripts and the full before/after tables live in the benchmarks/ directory (benchmarks/RESULTS.md).","title":"Assembly cost and quadrature reuse"},{"category":"section","location":"manual/performance/#Choosing-nquad","page":"Performance Guide","text":"For well-separated elements, nquad=4 is a good default — nquad² = 16 quadrature points on each element, hence nquad⁴ = 256 point-pairs per element pair. Increase nquad when:\n\nElements are large relative to their separation distance\nYou need sub-percent accuracy on individual element-pair values\nRow sums deviate noticeably from the expected value\n\nA convergence study is the most reliable guide:\n\nfor n in [2, 4, 6, 8, 12]\n    result = compute_view_factors(mesh; nquad=n, verbose=false)\n    i = findfirst(==(\"emitter\"),  result.group_names)\n    j = findfirst(==(\"receiver\"), result.group_names)\n    @printf(\"nquad=%2d  F12=%.7f\\n\", n, result.F_group[i,j])\nend","title":"Choosing nquad"},{"category":"section","location":"manual/performance/#Choosing-n_samples-for-Monte-Carlo","page":"Performance Guide","text":"The MC standard error scales as σ/√N where σ is the per-pair standard deviation. For smooth geometries σ is small and n_samples=10000 gives ~1% relative error. For geometries with many obstructions or near-singular pairs, σ is larger and n_samples=100000 or more may be needed.\n\nA rough guide for the parallel-plates benchmark (well-separated, no obstructions):\n\nn_samples Approximate error\n1000 ~10%\n10000 ~1%\n100000 ~0.3%\n1000000 ~0.1%","title":"Choosing n_samples for Monte Carlo"},{"category":"section","location":"manual/performance/#Ray-shooting-Monte-Carlo-(raytracetrue)","page":"Performance Guide","text":"For large or obstructed 3D meshes, raytrace=true is usually the fastest option: on a 72-point subset of the Howell catalog benchmark it was 2.9× faster than quadrature and 26.4× faster than pair-area Monte Carlo at n_samples=5000 (see benchmarks/howell/RESULTS.md). It scales as O(N · n_rays · log N) rather than O(N²), and — unlike pair-area Monte Carlo — does not need a separate obstruction check per pair, since every radiating element is already part of the one scene BVH each ray is cast against.\n\nn_rays (default 10000) counts rays per element, not per element pair, so it is not directly comparable to n_samples — tune it per case. Increase it for smoother row sums on a closed enclosure (row sums converge to 1 as ordinary MC noise shrinks with n_rays) or for finer per-pair accuracy; decrease it for a quick estimate. On coarsely faceted curved bodies (spheres, cylinders), increasing n_rays alone will not remove the small systematic faceting bias described in Integration Methods — refine the mesh instead.","title":"Ray-shooting Monte Carlo (raytrace=true)"},{"category":"section","location":"manual/performance/#Duffy-vs-high-nquad","page":"Performance Guide","text":"For inclined-plate geometries with a shared edge, use_duffy=true with nquad=4 typically outperforms use_duffy=false with nquad=16 in both accuracy and runtime. The Duffy path evaluates 5 × nquad⁴ points per shared edge — with nquad=4 that is 1280 per pair vs 65536 for nquad=16 standard.","title":"Duffy vs high nquad"},{"category":"section","location":"manual/performance/#Threading","page":"Performance Guide","text":"The CPU path uses Threads.@threads over element rows. Set the thread count before starting Julia:\n\njulia --threads=8 script.jl\n# or\nJULIA_NUM_THREADS=8 julia script.jl\n\nFor Monte Carlo, one independent RNG is pre-generated per row (seeded from the rng you pass), so results are reproducible and free of lock contention regardless of how Julia schedules the threads.","title":"Threading"},{"category":"section","location":"manual/performance/#Memory","page":"Performance Guide","text":"The F_elem matrix is dense with N² Float64 values. For N = 1000 elements this is ~8 MB; for N = 10000 it is ~800 MB. If memory is a concern, consider aggregating to group level and discarding F_elem:\n\nresult   = compute_view_factors(mesh; nquad=4)\nF_groups = result.F_group   # keep this\nresult   = nothing           # allow F_elem to be garbage collected\nGC.gc()","title":"Memory"},{"category":"section","location":"manual/performance/#GPU-transfer-cost","page":"Performance Guide","text":"The host↔device transfer of mesh coordinates and node indices is a one-time cost at the start of compute_view_factors. For very large meshes the BVH (if used) is also transferred once. These costs are amortised over the N(N-1)/2 element pair computations that follow.\n\nFor repeated computations on the same mesh (e.g. parameter sweeps), consider keeping the mesh data resident on the device. This requires lower-level access to build_gpu_arrays from GPUKernels — contact the package author for guidance.","title":"GPU transfer cost"},{"category":"section","location":"manual/getting_started/#Getting-Started","page":"Getting Started","text":"","title":"Getting Started"},{"category":"section","location":"manual/getting_started/#Loading-a-mesh","page":"Getting Started","text":"load_mesh reads any format the Gmsh SDK can open — .msh (v2.2 and v4), .stl, .step/.stp, Nastran, .med, legacy .vtk, and more — and auto-detects XML VTK (.vtu) files, which are read through ReadVTK.jl. Meshes may be structured or unstructured, with 1st- or 2nd-order elements (or a mix).\n\nRadiating geometry is partitioned by named groups (Physical Surface in 3D, Physical Curve in 2D). Formats without named groups (e.g. STL) fall back to a single \"default\" group.\n\nusing RadiativeViewFactor\n\n# 3D surface mesh (default)\nmesh = load_mesh(\"geometry.msh\")\n\n# 2D planar curve mesh\nmesh = load_mesh(\"planar.msh\"; surface_dim=1)\n\n# Other formats are detected from the extension\nmesh = load_mesh(\"part.stl\")\nmesh = load_mesh(\"assembly.step\")\n\n# XML VTK (.vtu) requires ReadVTK in scope\nusing ReadVTK\nmesh = load_mesh(\"grid.vtu\")","title":"Loading a mesh"},{"category":"section","location":"manual/getting_started/#Computing-view-factors","page":"Getting Started","text":"result = compute_view_factors(mesh; nquad=4)\n\nThis returns a ViewFactorResult containing view factors at both the element level (F_elem) and the physical-group level (F_group).","title":"Computing view factors"},{"category":"section","location":"manual/getting_started/#Reading-the-result","page":"Getting Started","text":"The rows and columns of F_group correspond to physical groups in the order given by result.group_tags and result.group_names:\n\n# Print all group names\nprintln(result.group_names)\n\n# Look up a specific pair by name\ni = findfirst(==(\"hotplate\"),  result.group_names)\nj = findfirst(==(\"coldplate\"), result.group_names)\nprintln(\"F(hotplate → coldplate) = \", result.F_group[i, j])\n\nF_group[i, j] is the view factor from group i to group j.","title":"Reading the result"},{"category":"section","location":"manual/getting_started/#Validation-checks","page":"Getting Started","text":"check_reciprocity(result)   # verifies Aᵢ Fᵢⱼ ≈ Aⱼ Fⱼᵢ\ncheck_closure(result)       # verifies row sums ≤ 1\n\nRow sums less than 1 are expected for open geometries where radiation escapes through open boundaries.","title":"Validation checks"},{"category":"section","location":"manual/obstruction/#Obstruction-Detection","page":"Obstruction Detection","text":"Obstruction detection determines whether the line of sight between two quadrature (or sample) points is blocked by a third surface, setting H_ij = 0 in the view factor integral for blocked pairs.","title":"Obstruction Detection"},{"category":"section","location":"manual/obstruction/#Enabling-obstruction","page":"Obstruction Detection","text":"Pass the physical group tags of all surfaces that may block rays:\n\nresult = compute_view_factors(mesh; nquad=4, obstruction_groups=[3, 4])\n\nThe source and destination groups are automatically excluded from the obstruction geometry for each element pair — a surface never blocks rays that originate from or terminate on itself. You may safely include all group tags in obstruction_groups without worrying about self-blocking:\n\nall_tags = collect(keys(mesh.group_tags))\nresult   = compute_view_factors(mesh; nquad=4, obstruction_groups=all_tags)","title":"Enabling obstruction"},{"category":"section","location":"manual/obstruction/#How-it-works","page":"Obstruction Detection","text":"CPU path: for each unique set of active obstruction groups (after excluding the source and destination groups), a BVH is built once from the merged triangle (3D) or line-segment (2D) soups of those groups and reused for all element pairs sharing that set. Ray–triangle intersection uses the Möller–Trumbore algorithm; ray–segment intersection uses 2D Cramer's rule.\n\nGPU path: the BVH is flattened to typed device arrays with miss-link pointers for stackless traversal. Each GPU thread traverses the BVH independently with no thread-local stack, eliminating register pressure from MVector storage. Per-triangle group tags stored in the BVH allow each thread to skip triangles belonging to the emitter or receiver group without host-side pre-filtering.","title":"How it works"},{"category":"section","location":"manual/obstruction/#Ray-shooting-Monte-Carlo-is-different","page":"Obstruction Detection","text":"raytrace=true builds one scene BVH from every radiating element plus obstruction_groups, so radiating elements obstruct each other automatically without being listed in obstruction_groups — that argument only adds extra non-radiating blocker geometry for this method. See Integration Methods.","title":"Ray-shooting Monte Carlo is different"},{"category":"section","location":"manual/obstruction/#Compatibility","page":"Obstruction Detection","text":"Obstruction detection works with quadrature, pair-area Monte Carlo, and Duffy the same way:\n\n# Quadrature + obstruction\nresult = compute_view_factors(mesh; nquad=4, obstruction_groups=[3])\n\n# Monte Carlo + obstruction\nresult = compute_view_factors(mesh; monte_carlo=true, n_samples=50000,\n                               obstruction_groups=[3])\n\n# Duffy + obstruction\nresult = compute_view_factors(mesh; nquad=6, use_duffy=true,\n                               obstruction_groups=[3])\n\n# GPU + obstruction\nusing CUDA\nresult = compute_view_factors(mesh; nquad=4, backend=CUDABackend(),\n                               obstruction_groups=[3])","title":"Compatibility"},{"category":"section","location":"manual/obstruction/#Performance","page":"Obstruction Detection","text":"The BVH query is O(log N_tris) per ray. For meshes with many obstruction triangles and high nquad, the BVH cost can dominate the integration cost. Monte Carlo integration is often faster than quadrature when obstructions are dense, because MC skips the BVH for pairs with K = 0 (back-facing pairs) without evaluating the BVH at all.","title":"Performance"},{"category":"section","location":"citing/#Citing-RadiativeViewFactor.jl","page":"Cite this repo","text":"If you use RadiativeViewFactor.jl in your research, please cite it. A \"Cite this repository\" button on the GitHub repository page generates formatted citations automatically from the CITATION.cff file.","title":"Citing RadiativeViewFactor.jl"},{"category":"section","location":"citing/#BibTeX","page":"Cite this repo","text":"@software{coxe_radiativeviewfactor_2026,\n  author  = {Coxe, Alexander M.},\n  title   = {{RadiativeViewFactor.jl}},\n  url     = {https://github.com/rot4te/RadiativeViewFactor.jl},\n  version = {0.6.3},\n  year    = {2026}\n}","title":"BibTeX"},{"category":"section","location":"citing/#Key-references","page":"Cite this repo","text":"The numerical methods implemented in this package are described in the following works. Please also consider citing the relevant references when using specific features:\n\nDuffy transformation (use_duffy=true, and the automatic near-pair patch under monte_carlo=true): the implementation is a \"biggest-coordinate\" generalization of Duffy's original transform, not the Sauter–Schwab region formulas — the latter is included below as related background reading.\n\n@article{duffy_1982,\n  author  = {Duffy, M. G.},\n  title   = {Quadrature over a pyramid or cube of integrands with a\n             singularity at a vertex},\n  journal = {SIAM Journal on Numerical Analysis},\n  volume  = {19},\n  number  = {6},\n  pages   = {1260--1262},\n  year    = {1982},\n  doi     = {10.1137/0719090}\n}\n\n@book{sauter_schwab_2011,\n  author    = {Sauter, Stefan A. and Schwab, Christoph},\n  title     = {Boundary Element Methods},\n  publisher = {Springer},\n  year      = {2011},\n  doi       = {10.1007/978-3-540-68093-2}\n}\n\nView factor theory and analytical reference cases:\n\n@book{howell_2020,\n  author    = {Howell, John R. and Meng\\\"{u}\\c{c}, M. Pinar and Siegel, Robert},\n  title     = {Thermal Radiation Heat Transfer},\n  edition   = {7th},\n  publisher = {CRC Press},\n  year      = {2020}\n}\n\nRay–triangle intersection (obstruction detection):\n\n@article{moller_trumbore_1997,\n  author  = {M\\\"{o}ller, Tomas and Trumbore, Ben},\n  title   = {Fast, minimum storage ray/triangle intersection},\n  journal = {Journal of Graphics Tools},\n  volume  = {2},\n  number  = {1},\n  pages   = {21--28},\n  year    = {1997},\n  doi     = {10.1080/10867651.1997.10487468}\n}","title":"Key references"},{"category":"section","location":"manual/mesh_requirements/#Mesh-Requirements","page":"Mesh Requirements","text":"Meshes may be structured or unstructured and 1st- or 2nd-order, and the two orders may be mixed within a single mesh. The solver identifies elements by node connectivity alone — there is no structured-grid (i,j) assumption in assembly, quadrature, or the obstruction BVH — so an unstructured Delaunay triangulation, a structured/transfinite mesh, and a mixed-element mesh are all handled the same way.","title":"Mesh Requirements"},{"category":"section","location":"manual/mesh_requirements/#File-formats","page":"Mesh Requirements","text":"load_mesh accepts any format the Gmsh SDK can open, inferred from the extension: .msh (v2.2 and v4), .stl, .step/.stp, Nastran .bdf/.nas, .med, legacy .vtk, and others. XML VTK unstructured grids (.vtu, and XML-form .vtk) are detected automatically and read through ReadVTK.jl, which must be loaded (using ReadVTK).\n\nNek5000/NekRS .re2 binary meshes are also supported, through a dedicated in-tree parser (Gmsh cannot open them). A 3D hex volume mesh's boundary faces are extracted as radiating Quad4 surfaces, grouped by their Nek boundary-condition label, with normals oriented into the fluid cavity. Word size (4- or 8-byte reals) and byte order are auto-detected. See load_re2. Only 3D .re2 meshes are handled (surface_dim=2).\n\nRadiating geometry is partitioned by named groups. In Gmsh these are Physical Surface (3D) or Physical Curve (2D) groups. Formats that cannot carry named groups (e.g. STL) fall back to a single synthetic \"default\" group; for VTK, a per-cell integer region array can be used instead (see load_vtu). For .re2, the boundary-condition labels are the group names (internal E/P connectivity faces are skipped); if none are present the topological boundary is taken as a single \"default\" group.","title":"File formats"},{"category":"section","location":"manual/mesh_requirements/#Surface-meshes-(surface_dim2)","page":"Mesh Requirements","text":"Supported element types:\n\nOrder Gmsh type Name Nodes\n1st 2 Tri3 3 (linear triangle)\n1st 3 Quad4 4 (bilinear quadrilateral)\n2nd 9 Tri6 6 (quadratic triangle)\n2nd 16 Quad8 8 (serendipity quadrilateral) — preferred for curved geometry\n2nd 10 Quad9 9 (Lagrange quadrilateral) — centre node silently dropped\n\nRequirements:\n\nRadiating surfaces in named groups; obstruction surfaces in separate groups.\nMesh.ElementOrder selects the order (1 = default → Tri3/Quad4; 2 → Tri6/Quad8).\nElement normals follow node winding, which Gmsh keeps consistent within a surface. Opposing surfaces must be wound to face each other. No automatic orientation is applied for surface meshes; use reverse_normals=true to flip all normals at load time if a mesh comes in back-to-front.","title":"Surface meshes (surface_dim=2)"},{"category":"section","location":"manual/mesh_requirements/#Curve-meshes-(surface_dim1)","page":"Mesh Requirements","text":"Supported element types:\n\nOrder Gmsh type Name Nodes\n1st 1 Line2 2 (linear line)\n2nd 8 Line3 3 (quadratic line)\n\nRequirements:\n\nRadiating curves in named groups.\nMesh.ElementOrder selects the order (1 → Line2; 2 → Line3).\nNormal orientation is corrected automatically at load time (see below).","title":"Curve meshes (surface_dim=1)"},{"category":"section","location":"manual/mesh_requirements/#Normal-orientation-for-curve-meshes","page":"Mesh Requirements","text":"For surface_dim=1, element normals are computed by rotating the tangent vector 90° counter-clockwise in the xy-plane. The correct sign depends on how the curve is wound.\n\nRadiativeViewFactor.jl corrects this automatically at load time by locating the adjacent surface for each curve from mesh connectivity (shared nodes, then the nearest surface centroid), not from CAD topology. Because it reads connectivity rather than requiring a structured/transfinite mesh, it works for unstructured curve meshes too, and with .msh v2.2 files that carry no CAD topology. Any element whose normal points away from that surface interior is flipped.\n\nIf the auto-correction is wrong for a mesh, flip all normals at load time:\n\nmesh = load_mesh(\"planar.msh\"; surface_dim=1, reverse_normals=true)\n\nreverse_normals is applied after the auto-correction. Use plot_mesh_normals to inspect normal directions before computing.","title":"Normal orientation for curve meshes"},{"category":"section","location":"manual/mesh_requirements/#Example-Gmsh-scripts","page":"Mesh Requirements","text":"// 2D curve mesh. Order 1 (default) gives Line2; set 2 for Line3.\nMesh.ElementOrder = 1;\n\nPhysical Curve(\"emitter\")     = {1};\nPhysical Curve(\"receiver\")    = {2};\nPhysical Curve(\"obstruction\") = {3};\n\n// 3D surface mesh. Order 1 (default) gives Tri3/Quad4; set 2 for Tri6/Quad8.\n// Recombination is optional — unstructured triangles are fully supported.\nMesh.ElementOrder    = 2;\nMesh.RecombineAll    = 1;   // produces quads instead of triangles\nMesh.Algorithm       = 8;   // Frontal-Delaunay for quads\n\nPhysical Surface(\"hotplate\")  = {1};\nPhysical Surface(\"coldplate\") = {2};\nPhysical Surface(\"fin\")       = {3};","title":"Example Gmsh scripts"},{"category":"section","location":"theory/#Theory","page":"Theory","text":"","title":"Theory"},{"category":"section","location":"theory/#View-factor-definition","page":"Theory","text":"The view factor F_ij from surface i to surface j is the fraction of diffuse radiation leaving surface i that arrives at surface j:\n\n3D (surface meshes):\n\nF_ij = frac1A_i iint_A_i iint_A_j\n    fraccostheta_i costheta_jpi r^2  H_ij  dA_j  dA_i\n\n2D (curve meshes):\n\nF_ij = frac1L_i int_L_i int_L_j\n    fraccostheta_i costheta_j2r  H_ij  dL_j  dL_i\n\nwhere:\n\ntheta_i is the angle between the outward normal at dA_i and the line of sight r_ij\ntheta_j is the angle between the outward normal at dA_j and the reverse line of sight\nr is the distance between the two differential elements\nH_ij in 0 1 is the visibility function (0 = obstructed)\n\nThe factor 2 rather than pi in the 2D kernel follows from integrating the 2D radiation intensity over the hemisphere, which gives pi2 rather than pi.","title":"View factor definition"},{"category":"section","location":"theory/#Reciprocity","page":"Theory","text":"The reciprocity relation:\n\nA_i F_ij = A_j F_ji\n\nis a consequence of the symmetry of the kernel and holds exactly in the continuous case. At the discrete (element) level, reciprocity holds to within quadrature error; verify with check_reciprocity.","title":"Reciprocity"},{"category":"section","location":"theory/#Gauss–Legendre-quadrature","page":"Theory","text":"The isoparametric map mathbfx(xi eta) transforms the reference element -11^2 to physical space. The integral over one Quad8 element becomes:\n\nint_A f  dA = int_-1^1 int_-1^1\n    f(mathbfx(xieta)) leftfracpartialmathbfxpartialxi\n    times fracpartialmathbfxpartialetaright dxi  deta\n    approx sum_p=1^n^2 w_p  f(mathbfx(xi_peta_p))  J_p\n\nwhere J_p = partial_xi mathbfx times partial_eta mathbfx is the area Jacobian. A tensor-product n times n Gauss–Legendre rule is used.","title":"Gauss–Legendre quadrature"},{"category":"section","location":"theory/#Duffy-transformation","page":"Theory","text":"For element pairs sharing a vertex at mathbfu_0 = (u_0 v_0) in element i's unit square and mathbfs_0 = (s_0 t_0) in element j's, the kernel diverges as r to 0. Near the singularity:\n\nK sim frac1r^2 quad r sim sqrt(u-u_0)^2+(v-v_0)^2+(s-s_0)^2+(t-t_0)^2\n\nThe implementation uses an elementary \"biggest-coordinate\" Duffy decomposition (a generalization of Duffy's original single-simplex transform), not the specific region formulas of Sauter & Schwab's boundary element method — a related but different decomposition of the same singularity, and still the reference for the underlying idea.\n\nCommon vertex (one shared corner node): shift coordinates to tilde u = u-u_0, etc., so the singularity sits at the origin of 01^4. Split the hypercube into 4 regions by which of (tilde u tilde v tilde s tilde t) has the largest magnitude; in each region that coordinate is set to a radial variable rho in 01 and the other three to rho eta_k (eta_k in 01), giving a lower-triangular Jacobian with determinant rho^3:\n\nK cdot dA_i cdot dA_j cdot textJac sim frac1rho^2 cdot rho^2 cdot rho^3 = rho^3 to 0\nquad textas  rho to 0\n\nThe 4 regions exactly tile 01^4 (ties between coordinates have measure zero), each contributing 4 times nquad^4 total evaluation points.\n\nCommon edge (two shared corner nodes): in edge-local coordinates the singularity is the line u=s v=t=0 rather than a single point. Split the (us) square into the two triangles u geq s and u  s (Jacobian u or s respectively, a standard ratio parametrization); within each triangle the remaining three-variable point singularity (w v t) to 0 (where w = (u-s)u or (s-u)s) is resolved the same \"biggest-coordinate\" way, now with 3 regions and Jacobian rho^2. This gives 6 regions total (2 triangles × 3 sub-regions), each contributing 6 times nquad^4 total evaluation points, with combined Jacobian (utext or s) cdot rho^2.\n\nThe transformed integrand is smooth at rho = 0 in both cases and is efficiently resolved by standard Gauss–Legendre quadrature.","title":"Duffy transformation"},{"category":"section","location":"theory/#Monte-Carlo-estimator","page":"Theory","text":"The unbiased area-sampling estimator for each element pair (ij) is:\n\niint K  dA_j  dA_i\napprox fracA_i cdot A_jN sum_k=1^N\nKleft(x_i^(k) n_i^(k) x_j^(k) n_j^(k)right) cdot H_ij^(k)\n\nwhere (x_i^(k) x_j^(k)) are i.i.d. uniform samples on A_i times A_j.\n\nStratified sampling subdivides the reference square into lfloorsqrtNrfloor times lfloorsqrtNrfloor strata and draws one point per stratum. For smooth integrands, stratified sampling achieves O(1N) variance convergence rather than O(1sqrtN) for plain Monte Carlo, matching the rate of a 1D Gauss rule.\n\nVariance near singularities: for the 1r^2 kernel the variance of the MC estimator is proportional to iint K^2  dA, which diverges at shared edges. Infinite variance means no amount of increasing N gives reliable convergence — use the Duffy transformation instead for such pairs.","title":"Monte Carlo estimator"},{"category":"section","location":"theory/#Ray-shooting-Monte-Carlo-estimator","page":"Theory","text":"A second, unrelated Monte Carlo estimator (raytrace=true) rewrites the double-area integral as a single-area integral over the (occlusion-limited) solid angle Omega_j that surface j subtends from each point x in A_i:\n\nF_i to j = frac1A_i int_A_i\n    left int_Omega_j text visible from  x fraccostheta_ipi  dOmega right dA_i\n\nwhich follows from the solid-angle identity dA_j costheta_j  r^2 = dOmega. Drawing a ray direction omega from the cosine-weighted hemisphere pdf costheta_i  pi gives\n\nmathbbEbigmathbb1(textrays first hit is  j)big\n    = int_Omega_j text visible fraccostheta_ipi  dOmega = F_x to j\n\n— the hit indicator's expectation is exactly the point-to-area form factor, occlusion included, with no costheta_j, no 1r^2, and no separate visibility test: a ray hitting the wrong surface first is the visibility test. Averaging over points x drawn uniformly by area on A_i gives F_i to j directly.\n\nBecause this estimator never evaluates 1r^2, it has ordinary bounded (binomial) variance for every element pair, including adjacent ones — no Duffy patch is applied. Each unordered pair ij gets two independent estimates (one from each element's own rays); Assembly.jl averages them before dividing back out to F, which enforces reciprocity by construction rather than as an emergent property of enough samples. See src/RayTraceKernel.jl's module docstring for the full derivation and a front/back-face subtlety found while implementing it.","title":"Ray-shooting Monte Carlo estimator"},{"category":"section","location":"#RadiativeViewFactor.jl","page":"Home","text":"A Julia package for computing radiative view factors between arbitrary surfaces or curves. Meshes may be structured or unstructured and 1st- or 2nd-order; any Gmsh-readable format is supported, plus XML VTK (.vtu) via an optional ReadVTK.jl extension.","title":"RadiativeViewFactor.jl"},{"category":"section","location":"#Overview","page":"Home","text":"RadiativeViewFactor.jl evaluates the double-surface integral that defines the geometric view factor between pairs of finite surfaces. The geometry is supplied as a mesh: the solver works from element node connectivity alone, so structured and unstructured meshes are treated identically, and 1st- and 2nd-order elements may be mixed freely. Four integration strategies are available:\n\nGauss–Legendre quadrature — the default; spectral convergence for smooth geometries\nMonte Carlo (pair-area sampling) — stratified sampling of point pairs on each element pair; advantageous when many obstructions are present or near-singular pairs exist\nDuffy transformation — singularity-regularizing change of variables for Quad4/Quad8 element pairs sharing a vertex or edge; gives accurate results for inclined surfaces with common edges\nMonte Carlo (ray-shooting) — cosine-weighted rays shot from each element into the whole scene at once; the fastest method for large or obstructed 3D meshes, since obstruction and visibility fall out of the same BVH query\n\nQuadrature, pair-area Monte Carlo, and Duffy support obstruction detection via BVH-accelerated ray casting, and all four methods work on CPU; quadrature, both Monte Carlo variants, and obstruction work on GPU too (Duffy is CPU-only).","title":"Overview"},{"category":"section","location":"#Installation","page":"Home","text":"using Pkg\nPkg.add(url=\"https://github.com/rot4te/RadiativeViewFactor.jl\")\n\nFor GPU support, install the relevant backend before loading the package:\n\nPkg.add(\"CUDA\")   # NVIDIA\nPkg.add(\"Metal\")  # Apple Silicon\n\nFor mesh visualisation:\n\nPkg.add(\"Plots\")   # enables plot_mesh_normals\n\nFor loading XML VTK (.vtu) meshes:\n\nPkg.add(\"ReadVTK\")","title":"Installation"},{"category":"section","location":"#Quick-Example","page":"Home","text":"using RadiativeViewFactor\n\nmesh   = load_mesh(\"geometry.msh\")\nresult = compute_view_factors(mesh; nquad=4)\n\ni = findfirst(==(\"emitter\"),  result.group_names)\nj = findfirst(==(\"receiver\"), result.group_names)\nprintln(\"F(emitter → receiver) = \", result.F_group[i, j])\n\ncheck_reciprocity(result)\ncheck_closure(result)","title":"Quick Example"},{"category":"section","location":"#Contents","page":"Home","text":"Pages = [\n    \"manual/getting_started.md\",\n    \"manual/mesh_requirements.md\",\n    \"manual/integration_methods.md\",\n    \"manual/obstruction.md\",\n    \"manual/gpu.md\",\n    \"manual/visualisation.md\",\n    \"manual/performance.md\",\n    \"theory.md\",\n    \"api.md\",\n    \"references.md\",\n]\nDepth = 2","title":"Contents"},{"category":"section","location":"references/#References","page":"References","text":"The following works informed the numerical methods implemented in this package.","title":"References"},{"category":"section","location":"references/#View-factor-theory","page":"References","text":"Howell, J. R., Mengüç, M. P., & Siegel, R. (2020). Thermal Radiation Heat Transfer (7th ed.). CRC Press. — View factor definitions, reciprocity relation, crossed-string method for 2D geometries, and analytical reference cases used for validation.\nHamilton, D. C., & Morgan, W. R. (1952). Radiant interchange configuration factors. NACA Technical Note 2836. — Original tabulation of configuration factor formulae for standard geometries.","title":"View factor theory"},{"category":"section","location":"references/#Finite-element-geometry","page":"References","text":"Zienkiewicz, O. C., Taylor, R. L., & Zhu, J. Z. (2005). The Finite Element Method: Its Basis and Fundamentals (6th ed.). Elsevier. — Quad8 serendipity and Tri6 shape functions, isoparametric mapping, Gauss quadrature on reference elements.","title":"Finite element geometry"},{"category":"section","location":"references/#Singularity-treatment-(Duffy-transformation)","page":"References","text":"Duffy, M. G. (1982). Quadrature over a pyramid or cube of integrands with a singularity at a vertex. SIAM Journal on Numerical Analysis, 19(6), 1260–1262. — Original Duffy transformation. DuffyKernel.jl implements an elementary \"biggest-coordinate\" generalization of this single-simplex transform (4 regions for a common vertex, 6 for a common edge), not the specific Sauter–Schwab region formulas below.\nSauter, S. A., & Schwab, C. (2011). Boundary Element Methods. Springer. — Sauter–Schwab common-vertex (§5.3.2) and common-edge (§5.3.3) decompositions; a related but distinct 4D Duffy-type regularization for the same singularity, kept here as background reading rather than as the implemented method.","title":"Singularity treatment (Duffy transformation)"},{"category":"section","location":"references/#Gaussian-quadrature","page":"References","text":"Golub, G. H., & Welsch, J. H. (1969). Calculation of Gauss quadrature rules. Mathematics of Computation, 23(106), 221–230. — Golub–Welsch tridiagonal eigenvalue algorithm used in Quadrature.jl to generate n-point Gauss–Legendre rules for n > 5.\nDunavant, D. A. (1985). High degree efficient symmetrical Gaussian quadrature rules for the triangle. International Journal for Numerical Methods in Engineering, 21(6), 1129–1148. — Dunavant triangle quadrature rules used for Tri6 surface elements.","title":"Gaussian quadrature"},{"category":"section","location":"references/#Ray–triangle-intersection","page":"References","text":"Möller, T., & Trumbore, B. (1997). Fast, minimum storage ray/triangle intersection. Journal of Graphics Tools, 2(1), 21–28. — Möller–Trumbore algorithm implemented in BVH.jl for obstruction testing in 3D.","title":"Ray–triangle intersection"},{"category":"section","location":"references/#BVH-traversal","page":"References","text":"Shirley, P., et al. (2019). Ray Tracing Gems. Apress. — Stackless BVH traversal via miss-link (skip-pointer) encoding, implemented in GPUBVH.jl to eliminate per-thread stack memory on GPU.","title":"BVH traversal"},{"category":"section","location":"references/#Monte-Carlo-integration","page":"References","text":"Pharr, M., Jakob, W., & Humphreys, G. (2023). Physically Based Rendering: From Theory to Implementation (4th ed.). MIT Press. — Stratified sampling, variance reduction, and Monte Carlo estimators for light transport integrals; basis for the stratified area-sampling scheme in MCKernel.jl.\nCohen, M. F., & Wallace, J. R. (1995). Radiosity and Realistic Image Synthesis. Academic Press. — Source for the solid-angle identity dA_j costheta_j  r^2 = dOmega underlying the cosine-weighted ray-shooting estimator in RayTraceKernel.jl and GPURayTraceKernels.jl.","title":"Monte Carlo integration"},{"category":"section","location":"references/#GPU-pseudo-random-number-generation","page":"References","text":"Marsaglia, G. (2003). Xorshift RNGs. Journal of Statistical Software, 8(14). — xorshift64 PRNG used in GPUMCKernels.jl for per-thread random streams with no heap allocation.\nSteele, G. L., Lea, D., & Flood, C. H. (2014). Fast splittable pseudorandom number generators. ACM SIGPLAN Notices, 49(10), 453–472. — splitmix64 mixing function used to derive independent per-thread seeds from the global seed and thread index.","title":"GPU pseudo-random number generation"}]
}
